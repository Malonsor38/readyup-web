# Convenience redirects only.
#
# Do NOT add /faq -> /faq.html rewrites here: Cloudflare's html_handling
# already serves faq.html at /faq, and the two rules fight each other.

/signup          /join         301
/sign-up         /join         301
/register        /join         301
/login           /join         301
/signin          /join         301
/sign-in         /join         301
/account         /edit         301
/settings        /edit         301
/my-profile      /edit         301
/rules           /guidelines   301
/code-of-conduct /guidelines   301
/help            /faq          301
/questions       /faq          301

# Public profiles: /jae-rivera serves profile.html, which reads the handle
# from ?u= and looks it up. Real files and pages are matched first, so this
# only catches addresses that aren't already something else.
# KEEP THIS LAST — anything below it will never be reached.
/*   /profile.html?u=:splat   200
