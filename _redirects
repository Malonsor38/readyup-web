# Convenience redirects only.
#
# Do NOT add /faq -> /faq.html rewrites here: Cloudflare's html_handling
# already serves faq.html at /faq, and the two rules fight each other.

/signup          /join         301
/sign-up         /join         301
/register        /join         301
/login           /join         301
/signin          /join         301
/sign-in         /join         301
/account         /edit         301
/settings        /edit         301
/my-profile      /edit         301
/rules           /guidelines   301
/code-of-conduct /guidelines   301
/help            /faq          301
/questions       /faq          301

# NOT YET: the public profile catch-all.
# Once the handle-driven profile page exists, add this as the LAST line:
#   /*   /profile.html?u=:splat   200
# Adding it now would serve the hardcoded example profile for every
# mistyped URL on the site.
